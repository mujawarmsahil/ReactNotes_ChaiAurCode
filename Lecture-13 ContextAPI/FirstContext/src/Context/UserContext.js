import { createContext } from "react"

const UserContext = createContext();

export default UserContext;

//In this file we are creating a context which is going to be called in jsx elements from where we are passing the parameters to the children  